import { css } from "@/styled-system/css";

export const spacerContainer = css({
    display: 'flex',
    width: '100%',
    justifyContent: 'center',
    alignItems: 'center',
    gap: '10px',

});

export const divider = css({
    flex: '1',
    height: '1px',
    backgroundColor: 'text.3',
});